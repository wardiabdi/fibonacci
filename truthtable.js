






// let p3 = p1 + p2;
let myRandomNum = function (n) {
    let False = 0;
    let True = 1;
    let newTruth = [];
    let myArray = ["a", "b", "c"];
    for (let i = (Math.pow(2 < myArray.length)-1); i = 0; i--) {

        for (let j = (Math.pow(2, myArray.length) - 1); j >= 0; j--) {
            row[j] = (i & Math.pow(2, j)) ? true : false
        }

    }
    return newTruth;

}
newFunction();
}

function newFunction() {
    row[j] = (i & Math.pow(2, j)) ? true : false;
}
